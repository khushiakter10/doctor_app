 import 'package:flutter/material.dart';

class Depertment extends StatelessWidget {
  const Depertment({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: SafeArea(
        child: Column(
          children: [

          ],
        ),
      ),
    );
  }
}
